/*
 * STUB — replaced by the header agent.
 * Contract: async server component, no props. Fetches whatever navigation data it
 * needs (categories, collections) and renders the announcement bar + header.
 */
export async function SiteHeader() {
  return null;
}
